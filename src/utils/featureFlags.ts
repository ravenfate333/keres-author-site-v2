/* Master switch for the genre sorter feature. Set this to `true` to 
    make the toggle option available in Sanity and to enable the feature on the frontend.
    For a client without feature in their package, set it to `false`. */

export const featureFlags = {
    // All books genre sorter
    enableGenreSorter: true,
};